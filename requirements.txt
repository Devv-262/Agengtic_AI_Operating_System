python-dotenv>=1.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
rich>=13.7.0

# Document parsing
pypdf>=4.0.0
python-docx>=1.1.0

# Windows system control (safe to install cross-platform; guarded at import time)
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"

# Floating desktop bar UI (python -m src.main --gui)
PyQt6>=6.6.0
keyboard>=0.13.5

# Semantic desktop search (local vector index + on-device embeddings, no API key needed)
chromadb>=0.5.0

# Task automation (image resize)
Pillow>=10.0.0
